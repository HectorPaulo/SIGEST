export interface FuncionInsert {
    clave: number;
    nombre: string;
    descripcion?: string;
}

export interface Funcion {
    id: string;
    clave: number;
    nombre: string;
    descripcion: string;
}